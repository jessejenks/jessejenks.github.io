<a
    aria-describedby="{{ include.name }}-label"
    id="{{ include.name }}-{{ include.id }}-label"
    href="#{{ include.name }}-{{ include.id }}"
    {% if include.title %}
    title="{{ include.title }}"
    {% endif %}
>{{ include.content }}</a>