<a
    target="_blank"
    rel="noreferrer"
    {% if include.href %}
        href="{{ include.href }}"
    {% endif %}
    {% if include.id %}
        id="{{ include.id }}"
    {% endif %}
    {% if include.title %}
        title="{{ include.title }}"
    {% endif %}
>{% if include.content %}{{ include.content }}{% endif %}</a>